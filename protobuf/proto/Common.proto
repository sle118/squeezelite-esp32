syntax = "proto3";

package sys.dev.common;
import "nanopb.proto";
option (nanopb_fileopt).enum_to_string = true;

enum types {
  UNKNOWN = 0;
  SPI = 1;
  I2C = 2;
  RMII = 3;
}

enum ports {
  UNSPECIFIED = 0;
  SYSTEM = 1;
  DAC = 2;
}

enum hosts {
  NONE = 0;
  Host0 = 1;
  Host1 = 2;
}
